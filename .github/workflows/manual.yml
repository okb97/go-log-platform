name: 2.5 Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        default: Hello
        required: true
        description: A cheerful word
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{  github.event.inputs.greeting  }}"
